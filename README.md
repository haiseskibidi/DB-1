# 🍳 CulinaryDB - Агрегатор кулинарных рецептов

**Автор:** Копаницкий Захар Александрович ПИКД 7

Проект представляет собой единую базу данных рецептов, собранных с популярных кулинарных сайтов: RussianFood, Povarenok и 1000.menu.

## 🚀 Технологический стек

*   **Backend:** Python 3.11, FastAPI, SQLAlchemy (SQLite/PostgreSQL).
*   **Frontend:** JavaScript, React, Vite.
*   **Парсинг:** Requests, BeautifulSoup4, fake-useragent.
*   **Инфраструктура:** Docker, Docker Compose.

## 🛠 Установка и запуск

Для запуска проекта вам понадобится только **Docker Desktop**.

### Шаг 1: Клонирование репозитория
```bash
git clone https://github.com/your-username/culinary-db.git
cd culinary-db
```

### Шаг 2: Запуск через Docker Compose
Выполните одну команду в корне проекта:
```bash
docker-compose up -d --build
```

После успешного запуска приложение будет доступно по адресу:
👉 **http://localhost:80**

API документация (Swagger):
👉 **http://localhost:8000/docs**

## 📂 Структура проекта

```
.
├── backend/          # Серверная часть (API)
│   ├── app/          # Логика приложения (models, schemas, main)
│   └── database/     # Файл базы данных (создается автоматически)
├── frontend/         # Клиентская часть (React)
├── parsers/          # Скрипты для сбора данных
│   ├── run_all.py    # Скрипт запуска всех парсеров
│   └── ...           # Парсеры для конкретных сайтов
├── data/             # Собранные "сырые" данные (JSON)
└── docker-compose.yml # Конфигурация контейнеров
```

## 🔄 Обновление данных (Парсинг)

Если вы хотите собрать свежие данные, вы можете запустить парсеры локально (требуется Python):

```bash
# Установка зависимостей
pip install -r parsers/requirements.txt

# Запуск сбора данных
python parsers/run_all.py
```
После этого перезапустите контейнеры, чтобы новые данные попали в базу.

## 📊 Статистика

На текущий момент в базе:
*   > 5000 рецептов
*   3 источника данных
*   Десятки категорий (Супы, Выпечка, Закуски и др.)


